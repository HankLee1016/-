# PostgreSQL 數據庫配置
# 請複制此文件為 .env 並填入実際的值

# 數據庫主機名 (例如: db.supabase.co)
DB_HOST=your_db_host

# 數據庫連接埠 (默認值: 5432)
DB_PORT=5432

# 數據庫使用者名稱
DB_USER=postgres

# 數據庫密碼
DB_PASSWORD=your_password

# 數據庫名稱 (默認值: postgres)
DB_NAME=postgres

# SSL 模式 (默認值: require)
DB_SSLMODE=require
