# 企劃書生成與審核工作流 - 完整測試指南

## 📌 工作流程概述

```
用戶填寫 4 步表單
    ↓
生成企劃書預覽（自動計算預算表與甘特圖）
    ↓
正式提交審核（保存到 applications.json）
    ↓
用戶查看申請狀態（/user/applications）
    ↓
管理員審核（/admin/applications）
    ↓
審核結果反饋（批准/拒絕 + 意見回複）
```

---

## 🚀 測試步驟

### 1️⃣ 用戶端 - 企劃書生成與提交

#### 前置條件
- 以普通用戶身份登入
- 訪問 `/user` 或 `/subsidy_application` 頁面

#### 步驟 1：填寫 Step 1 - 機構信息

表單字段：
- **機構名稱**：輸入你的機構名稱（如「○○社會福利基金會」）
- **機構類型**：選擇適當的類型
- **計畫名稱**：例如「弱勢家庭支持與社區連結計畫」
- **服務對象**：例如「低收家庭、新住民」

✅ 必填驗證：至少填入「機構名稱」和「計畫名稱」

#### 步驟 2：填寫 Step 2 - 補助與經費

表單字段：
- **補助來源**：選擇（如衛生福利部）
- **補助大類**：選擇（如社會救助）
- **補助方案名稱**：輸入補助方案

預算編輯器：
1. 點擊「新增預算項目」添加預算行
2. 輸入預算項目名稱、金額、說明
3. 系統自動計算合計金額

✅ 必填驗證：補助方案不能為空，至少一筆預算記錄

#### 步驟 3：填寫 Step 3 - 計畫內容與時程

主要表單字段：
- **計畫背景**：詳細描述服務需求
- **計畫目標**：具體列出目標
- **主要活動/執行策略**：詳述實施方式
- **預期效益**：說明計畫成果

時程編輯器：
1. 點擊「新增時程項目」
2. 填寫項目名稱、負責人、開始/結束日期、完成度 (%)
3. **甘特圖預覽會實時更新**

✅ 必填驗證：
- 計畫背景和目標不能為空
- 至少一筆完整的時程項目（包含有效日期）

#### 步驟 4：生成與提交

1. 點擊「AI 生成企劃書」按鈕
2. 系統會：
   - 整合所有表單數據
   - 生成正式的企劃書 HTML
   - 自動生成預算表
   - 自動生成月尺度甘特圖
3. 預覽結果包含：
   - 企劃書正式內容（7 個段落）
   - 預算表（HTML 格式）
   - 甘特圖（月尺度進度表）

匯出選項（可選）：
- **匯出 DOC**：下載 Word 格式文檔
- **匯出 PDF**：使用瀏覽器列印功能
- **複製預算表**：直接複製到 Excel/Word
- **複製甘特圖**：直接複製進度表

**✅ 正式提交**：
1. 點擊「正式提交審核」按鈕
2. 系統會：
   - 驗證所有必填字段
   - 將企劃書與表單數據保存到 `applications.json`
   - 生成唯一的 application_id
   - 記錄 created_at 時間戳
3. 成功後會顯示：
   - ✓ 成功消息
   - 自動重定向到 `/user/applications`

---

### 2️⃣ 用戶端 - 查看申請狀態

#### 訪問 `/user/applications`

顯示内容：
- **申請列表**：所有已提交的申請卡片
- **每個卡片包含**：
  - 案件名稱
  - 申請時間
  - **狀態標籤**：
    - ⏳ 待審核（黃色 badge）
    - ✓ 已批准（綠色 badge）
    - ✗ 已拒絕（紅色 badge）
  - 計畫背景摘要（前 150 字）
  - 管理者意見（如有）

#### 查看企劃書詳情

1. 點擊「查看企劃書」按鈕
2. 展開詳細企劃書内容
3. 點擊「列印企劃書」可直接列印

---

### 3️⃣ 管理員端 - 審核應用

#### 前置條件
- 以管理員身份登入（role = "admin"）
- 訪問 `/admin/applications`

#### 審核界面對象

每個待審核申請顯示：
- 申請者用戶名
- 案件名稱
- 申請時間
- **⏳ 待審核 badge**
- 補助信息
- 計畫背景
- 目標族群
- 計畫目標

#### 審核流程

**步驟 1：查看企劃書**
- 點擊「查看企劃書」按鈕展開完整企劃書内容
- 可以查看預算表和甘特圖

**步驟 2：批准申請**
```
1. 在「審核意見」文本框輸入批准意見（可選）
2. 點擊「✓ 批准此申請」按鈕
3. 系統會：
   - 將狀態改為 "approved"
   - 記錄管理員意見
   - 記錄更新時間戳
   - 用戶會在 /user/applications 看到 "✓ 已批准"
```

**步驟 3：拒絕申請**
```
1. 在「審核意見」文本框輸入拒絕理由（必填）
2. 點擊「✗ 拒絕此申請」按鈕
3. 系統會：
   - 將狀態改為 "rejected"
   - 記錄拒絕理由
   - 記錄更新時間戳
   - 用戶會在 /user/applications 看到 "✗ 已拒絕" 和理由
```

---

## 📊 數據流向

### applications.json 數據結構

```json
{
  "applications": [
    {
      "id": "uuid-string",
      "username": "user123",
      "case_title": "計畫名稱",
      "background": "詳細背景信息",
      "issues": "目標族群",
      "goals": "計畫目標",
      "proposal": "<html>企劃書 HTML 內容</html>",
      "subsidy_summary": "補助類別信息",
      "success_pdf": null,
      "subsidy_pdf": null,
      "status": "pending|approved|rejected",
      "admin_note": "管理員審核意見",
      "created_at": "2026-05-25T12:34:56.789012",
      "updated_at": "2026-05-25T14:30:00.123456"
    }
  ]
}
```

### API 端點

#### 生成企劃書
```
POST /api/generate-proposal
Content-Type: multipart/form-data

返回:
{
  "status": "success",
  "html_content": "<html>...</html>",
  "template_filename": "template_name.html",
  "gantt_included": true
}
```

#### 提交應用
```
POST /user/application/submit
Content-Type: multipart/form-data

返回:
{
  "status": "success",
  "message": "企劃書已成功提交",
  "application_id": "uuid",
  "redirect_url": "/user/applications"
}
```

#### 批准應用
```
POST /admin/applications/<application_id>/approve
Content-Type: application/x-www-form-urlencoded
Body: admin_note=審核意見

返回: 重定向到 /admin/applications
```

#### 拒絕應用
```
POST /admin/applications/<application_id>/reject
Content-Type: application/x-www-form-urlencoded
Body: admin_note=拒絕理由

返回: 重定向到 /admin/applications
```

---

## ✅ 完整流程測試清單

- [ ] **Step 1 驗證**
  - [ ] 填入全部必填字段，點擊「下一步」成功
  - [ ] 未填機構名稱或計畫名稱時，無法進行下一步

- [ ] **Step 2 驗證**
  - [ ] 添加預算項目，合計金額自動更新
  - [ ] 刪除預算項目，表單數據正確刪除

- [ ] **Step 3 驗證**
  - [ ] 時程項目添加成功，甘特圖實時預覽
  - [ ] 修改開始/結束日期，甘特圖立即更新
  - [ ] 完成度 % 正確顯示進度條

- [ ] **Step 4 - 生成**
  - [ ] 點擊「AI 生成企劃書」，預覽成功展示
  - [ ] 預算表完整顯示（項目名稱、金額、說明）
  - [ ] 甘特圖顯示月尺度的時程安排

- [ ] **提交**
  - [ ] 點擊「正式提交審核」，成功提交
  - [ ] 收到成功消息，2 秒後自動重定向

- [ ] **用戶查看**
  - [ ] 訪問 `/user/applications` 看到剛提交的申請
  - [ ] 狀態顯示「⏳ 待審核」
  - [ ] 點擊「查看企劃書」可展開詳細內容

- [ ] **管理員審核 - 批准**
  - [ ] 管理員訪問 `/admin/applications`
  - [ ] 看到保留的申請卡片
  - [ ] 填寫審核意見（可選），點擊「✓ 批准」
  - [ ] 申請狀態變為「✓ 已批准」
  - [ ] 用戶在 `/user/applications` 看到批准狀態和意見

- [ ] **管理員審核 - 拒絕**
  - [ ] 填寫拒絕理由（必填），點擊「✗ 拒絕」
  - [ ] 申請狀態變為「✗ 已拒絕」
  - [ ] 用戶在 `/user/applications` 看到拒絕狀態和理由

- [ ] **匯出功能**
  - [ ] 企劃書能匯出為 DOC
  - [ ] 企劃書能匯出為 PDF（列印功能）
  - [ ] 預算表能複製到剪貼板

---

## 🔧 故障排查

### 問題 1：提交時出現 403 Forbidden
**原因**：用戶未以普通用戶身份登入
**解決**：確保 `session.get("role") == "user"`

### 問題 2：應用未保存到 applications.json
**原因**：後端驗證失敗
**解決**：檢查 Console 錯誤信息，確認都有填寫必填字段

### 問題 3：甘特圖未顯示
**原因**：時程項目日期無效或格式錯誤
**解決**：確認日期為 `YYYY-MM-DD` 格式，且結束日期 >= 開始日期

### 問題 4：管理員看不到待審核的應用
**原因**：應用尚未成功提交
**解決**：檢查 applications.json 是否有新記錄

---

## 📝 重要說明

- 所有企劃書内容是 HTML 格式，能保留格式在 Word/PDF 中
- 預算和時程數據以 JSON 格式保存，方便後續處理
- 甘特圖使用月尺度，最多支持 24 個月的顯示
- 管理員審核意見會保存於 `admin_note` 欄位，用戶能查看

---

## 新建功能總結

✨ **此次改進包含**：

1. ✅ 前端提交按鈕與確認邏輯
2. ✅ 後端 JSON 響應支持
3. ✅ 用戶申請狀態查看界面改進
4. ✅ 管理員審核界面優化
5. ✅ 完整的狀態流追蹤
6. ✅ 審核意見反饋機制
